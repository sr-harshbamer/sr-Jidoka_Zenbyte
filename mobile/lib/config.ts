// The phone and the backend laptop must be on the same network (e.g. the
// laptop's own hotspot) for this to resolve during a demo. Update this IP
// to match whatever machine is running `npm run dev` in /backend.
export const API_BASE_URL = "http://10.73.122.43:4000";
